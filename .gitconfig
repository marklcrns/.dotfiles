[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	email = marklcrns@gmail.com
	name = Mark Lucernas
[credential]
	helper = cache
